[package]
name = "eech-sys"
version = "0.1.0"
description = "Private native layer: the original EECH campaign C, compiled for the host target, and its raw FFI. Consumed only by eech-campaign."
edition.workspace = true
publish.workspace = true
links = "eech_kernel"
build = "build/main.rs"

[build-dependencies]
cc = "1.2"
regex = "1"
