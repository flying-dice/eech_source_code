[package]
name = "eech-campaign"
version = "0.1.0"
description = "The EECH dynamic campaign as an embeddable native module: the public, safe Rust API."
edition.workspace = true
publish.workspace = true

[features]
# the legacy scenario replay (eech-core-ts C reference scenarios), for conformance tooling
conformance = []

[dependencies]
eech-sys = { path = "../eech-sys" }
serde = { workspace = true, optional = true }
